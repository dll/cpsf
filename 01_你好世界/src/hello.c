/*
 * 第1阶段：你好世界
 * 最简单的C语言程序 —— 打印字符串
 *
 * 本阶段目标：
 *   1. 理解C程序的基本结构
 *   2. 学会编译和运行C程序
 *   3. 认识表达式和函数调用
 *
 * 进化起点：从一行 printf 开始，我们将一步步走向插件框架
 */

#include <stdio.h>

/*
 * main 函数 —— C程序的入口点
 * 所有C程序都从 main 函数开始执行
 */
int main(void)
{
    /*
     * printf —— 标准库函数，用于格式化输出
     * "Hello, Plugin Framework!" 是字符串常量
     * \n 是换行符
     *
     * 思考：这行代码背后发生了什么？
     * - 我们调用了一个"别人写好的函数"
     * - 我们不需要知道 printf 内部怎么实现，只需要知道怎么用
     * - 这就是最朴素的"接口"思想
     */
    printf("Hello, Plugin Framework!\n");

    /* 返回0表示程序正常结束 */
    return 0;
}
