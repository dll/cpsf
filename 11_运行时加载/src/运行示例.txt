$ host.exe
------------------------------------------------------------
============================================================
  第11讲：运行时加载 —— ATM 插件宿主程序
============================================================
  宿主启动时并不认识任何插件；
  是下面的代码在运行中主动扫描、加载、调用、卸载。
------------------------------------------------------------

[步骤 1] 扫描插件目录：plugins
------------------------------------------------------------
  共发现 4 个候选插件文件

  发现文件：plugins\broken_plugin.dll
    ✗ 该文件缺少约定符号 "plugin_execute"，不是合法插件，已跳过

  发现文件：plugins\deposit_plugin.dll
    → 加载成功，插件名字：存款插件
    → 调用 plugin_execute(500.00)：
    [存款插件] 存入 500.00 元，当前余额 1500.00 元
    → 已卸载 plugins\deposit_plugin.dll

  发现文件：plugins\query_plugin.dll
    → 加载成功，插件名字：查询插件
    → 调用 plugin_execute(500.00)：
    [查询插件] 当前余额：1000.00 元
    → 已卸载 plugins\query_plugin.dll

  发现文件：plugins\withdraw_plugin.dll
    → 加载成功，插件名字：取款插件
    → 调用 plugin_execute(500.00)：
    [取款插件] 取出 500.00 元，当前余额 500.00 元
    → 已卸载 plugins\withdraw_plugin.dll

[步骤 2] 演示：故意加载一个不存在的库（看宿主怎么优雅报错）
------------------------------------------------------------
  发现文件：plugins\not_exist_plugin.dll
    ✗ 加载失败（已优雅跳过）：找不到指定的模块。

[步骤 3] 演示：在正常库里查找一个不存在的符号
------------------------------------------------------------
  在 plugins\deposit_plugin.dll 中查找符号 "plugin_exectue"（故意拼错）...
    ✗ 未找到（返回 NULL），宿主没有崩溃，正常继续。
    → 已卸载

============================================================
  运行结束：本轮成功加载并调用了 3 个插件
  所有插件都已卸载，宿主进程退出前不会留下任何库占用。
============================================================

  【本讲留下的新痛点】
    宿主是靠字符串 "plugin_execute(double)" 硬编码去调用的：
      - 查询插件根本不需要金额，却被迫接收 amount；
      - 想加一个"转账"插件（要两个账户+金额），一个 double 不够；
      - 每加一种能力，宿主都得改代码去认新的符号名。
    → 只约定"函数名"还不够，还需要约定"统一的接口"。
    → 这正是第12讲"接口抽象"要解决的问题。
============================================================

------------------------------------------------------------
退出码: 0
