# AI化教学创新03 · 任务复盘：主要提示词与关键步骤

> 项目：CPSF —— 从程序员到架构师 · C语言插件框架演进之旅
> 仓库：`github.com:dll/cpsf`（main 分支）
> 复盘日期：2026-09-24

---

## 一、任务是什么

把"C语言知识"讲成一门"软件架构演进史"。课程从一行 `printf("Hello World")` 出发，用 14 讲把同一套 ATM 银行案例逐级重构，每一讲只引入一个新概念、只解决上一阶段的真实痛点，最终落成一个可插拔的插件框架。

主线只有一条：

```
表达式 → 控制结构 → 函数 → 多文件 → 指针 → 数组 → 结构体 → 链表
      → 静态库 → 动态库 → 运行时加载 → 接口抽象 → 插件框架 → 完整项目
```

面向大三计算机专业学生（已学 C 语言、面向对象、数据结构），教学法采用"三个三分之一"：前段传统讲授、中段 AI 辅助、后段做中学。

---

## 二、主要提示词

按提出时间与主题归类。方框内为诉求原文或紧贴原意的概括。

### 2.1 立项与总纲

| # | 提示词 | 产生的决定 |
|:--:|:---|:---|
| P1 | 「找到当前目录下的 AI化教学创新 目录，并在其下新建 AI化教学创新03」 | 项目落位与目录名 |
| P2 | 「从 C 语言的表达式……一直演变到软件框架、插件框架，每一个重要的演变都创建一个文件夹目录，里面要有完整的代码、源代码解释，同时配上对应的文档（课件 + 简单的视频）」 | 全系列的基本形态：一讲一目录、代码 + 文档 + 视频三件套 |
| P3 | 「只用 C 语言」「目标受众是大三学生」 | 技术边界与难度定位 |
| P4 | 「库的话，我们可以再分出静态库和动态库」「多文件之后应该是链表」 | 讲次切分，最终定为 14 讲 |
| P5 | 每个阶段按「序号 + 关键字」命名，主要是中文 | 目录命名规范 `NN_主题` |
| P6 | 「这个我要形成一个系列，就是从程序员到软件框架」 | 系列定位与整体调性 |
| P7 | 仓库名为 CPSF（C 语言 Plugin 软件框架） | 仓库命名 |

### 2.2 内容与形式约定

| # | 提示词 | 产生的决定 |
|:--:|:---|:---|
| P8 | 「视频讲解采用录屏模式，给人一种操作应用程序、执行、运行、显示效果、输入输出的真实画面」 | 视频要有真实运行画面，不是纯 PPT 轮播 |
| P9 | 「课件呢，Markdown 即可，PPT 两个文件」 | 双课件：`课件.md` + `课件.pptx` |
| P10 | 「每一个都出一个测试题……思辨性质的，让他去思考」 | 每讲配 4 道思考题 |
| P11 | 「每个视频的长度大概在 5~7 分钟」「可以，这个采用 333 的交错机制」 | 时长区间 + 三个三分之一教学法 |
| P12 | 「最后每一讲形成一个知识库，在总知识库里面一点点将各讲的内容接入知识图谱」 | `知识图谱总览.md` 逐讲接入知识节点 |

### P13 | 关于视觉调性

> 「视频的课件背景色……不要那个科技感、黑色，这种味道太浓。要做得轻松愉快，讲解得通俗易懂又有趣，教师和学生都喜爱。」

这一条定下了全系列的视觉基调：暖橙色（`#FF8C42`）主色、薄荷绿（`#4ECDC4`）辅色、明亮浅底、卡通化卡片，不用深色科技风。

### P14 | 关于实战案例

> 「最后的实战案例，我想你弄个银行的 ATM 系统，非常简单的……业务交流就可以了。」

ATM 成为贯穿 14 讲的唯一案例，每一讲都在它身上做一次重构。

### P15 | 关于第一讲的返工

> 「我看了第一个视频，太虚假。你要首先突出表达式这个概念……一个表达式，我们写成文件之后进行编译、链接，输出得到的这样一个表达式的应用，这个要突出出来。编译是什么？链接是什么？是从库当中去调用 printf 函数，最后调用这个函数输出 —— 这个要解释清楚，不能囫囵吞枣。」

由此确立了"第 1 讲必须把编译四阶段与链接讲透"的硬要求。

### 2.3 视频与配音

| # | 提示词 | 产生的决定 |
|:--:|:---|:---|
| P16 | 「视频的声音能不能克隆我的声纹，用我的声音来进行讲解」 | 引入语音克隆 |
| P17 | 「豆包火山引擎具有声音克隆……voicebox 你可以完成」 | 本地 Voicebox 服务 + Qwen TTS 0.6B |
| P18 | 「Voice Box 生成的应该是声纹文件吧？在当前目录下的 刘东良.zip 压缩包里」 | 克隆音源与 profile 配置 |
| P19 | 「视频 3~5、6、7 分钟，你要把这个课件的思考题的相关知识好好地解释一下，而不是仅仅对这个程序代码进行解读，我们是从程序语言要说到软件架构，这个大的背景要补充丰富一下」 | 讲解稿必须上升到架构背景 |
| P20 | 每讲标题改成「第一讲表达式视频」这类自解释命名 | 课件被拷贝后仍可辨认 |
| P21 | 「确实音频太慢，后面的视频直接合成语音吧」 | 降级方案：改用 Windows SAPI 合成音 |
| P22 | 「后面的各讲，生成视频之外的所有资料可以并行生成」 | 并行生产策略 |

### 2.4 质量整改（投影与视频）

| # | 提示词 | 产生的决定 |
|:--:|:---|:---|
| P23 | 「课件标题不换行、彩色但清晰、前后不分要修、投影要看得清」 | 三条排版硬规矩 + 逐页质量门禁 |
| P24 | 「所有视频字幕、语音、内容、鼠标同步，PPT 与视频都要有思考题及解答」 | 三同步 + 思考题进 PPT 与视频 |
| P25 | 「视频 5~13 分钟，片尾要演示动画 / 录屏 / 截图」 | 时长区间放宽 + 片尾真实运行演示段 |
| P26 | 「第 1 讲画面抖动要处理、字幕要与页面内容一致」 | 逐帧亚像素推近 + 字幕符号还原 |
| P27 | 「第 2 讲起比较简化，各讲内容与长度要大体一致」 | 讲解稿统一加厚到 2870~2930 字 |
| P28 | 「每完善一讲就提交推送」 | 逐讲提交的节奏 |

### 2.5 代码与仓库规范

| # | 提示词 | 产生的决定 |
|:--:|:---|:---|
| P29 | 「后面几集代码不全，补充各讲对应的代码和构建脚本以及产物；程序中文乱码要解决」 | 14 讲代码全量补齐 + 中文乱码两层根治 |
| P30 | 「删除日志，整理规范，最后提交推送」 | 仓库瘦身约 1.7G + 四件套目录规范 |

### 2.6 综合篇与归档

| # | 提示词 | 产生的决定 |
|:--:|:---|:---|
| P31 | 「综合 14 讲，生成一期综合性、涵盖所有要点的视频，从表达式到框架，只要视频，放当前目录」 | 11 分钟综合篇成片 |
| P32 | 「对应视频，生成 PPTX 课件，然后就归档知识并提交推送」 | 综合篇课件归档 + 新建 `15_综合篇/` |

---

## 三、关键步骤

### 3.1 搭骨架：14 讲结构与知识图谱

先把"讲什么"定死，再动手做"怎么做"。目录采用 `NN_主题` 两段式命名，序号保证学习顺序，关键字保证脱离上下文也能看懂。每讲的固定结构为：

```
<NN_主题>/
├── README.md        讲次导读（目标 / 目录结构 / 前后衔接）
├── 思考题.md         4 道思考题 + 解答
├── docs/            课件.md · 课件.pptx · generate_ppt.py
├── src/             源码 + console_utf8.h + build.bat + Makefile + README.md + 运行示例.txt
└── video/           讲解.mp4 · 讲解.srt · 讲解脚本.json · video_meta.json + 配音源
```

`知识图谱总览.md` 承担跨讲串联：每讲完成后把新节点接入图谱，并在总览里维护 K 节点号与前后依赖。最终补入 K04~K14 共 40 个知识节点。

### 3.2 做内容：源码 → 课件 → PPT → 视频 → 思考题

每一讲的产能顺序固定为：先写 C 源码并跑通（`gcc -Wall` 零警告），源码跑通后才写课件——保证课件里贴的运行输出是真实的；课件定稿后生成 PPT；PPT 定稿后写讲解稿与配音；最后补思考题与 README。

这样排序的原因是：讲解稿与 PPT 都要引用真实源码与真实输出，源码不先行就会写出"看着对但跑不对"的内容。

### 3.3 视频流水线

自研工具链替代通用方案，`tools/` 下的核心环节：

| 环节 | 工具 | 关键做法 |
|:---|:---|:---|
| 排版与门禁 | `ppt_kit.py` | 逐页质量门禁，PIL 真实字体度量 + 6% 安全余量 |
| 导图 | `render_slides.py` | PPT 页导出为渲染底图 |
| 出片 | `build_video.py` | 段落边界 = 换页时机；PIL 硬字幕；鼠标缓动与点击涟漪；片尾真实运行演示 |
| 字幕还原 | `subtitle_text.py` | 口播读法还原成真实写法（"星号p" → `*p`、"arr方括号i" → `arr[i]`） |
| 字幕质检 | `check_subtitles.py` | 全 14 讲，要求 0 处待处理 |

段落划分以"换页"为唯一依据，让配音、画面、字幕天然对齐；片尾用真实程序的编译与运行输出做打字动画，兑现"录屏感"的要求。

### 3.4 配音策略

克隆音与合成音按讲次分工，并用文件指纹防止新旧混音：

| 讲次 | 音源 | 说明 |
|:---|:---|:---|
| 第 1、2、13、14 讲 | Voicebox 克隆音 | profile「刘东良」，Qwen TTS 0.6B，CPU 后端 |
| 第 3~12 讲 | Windows SAPI | `tools/tts_batch.py`，语速 196 |

`tools/_audiohash.py` 为每段配音写入"念稿 sha1"指纹，再加 mtime 兜底（音频必须比 `讲解脚本.json` 新）。讲稿一改，旧配音立即作废重做，杜绝"一半新稿一半旧稿"。

### 3.5 质量门禁

课件与视频各有闸门，不达标不出货：

- 排版三条硬规矩：标题不换行 / 层级不叠压 / 对比度 ≥ 4.5:1，另有"孤儿行"检查。
- 拼接前过时长闸：分段时长与配音时长相差超过 0.8 秒即重渲染，仍不达标就中止拼接。
- 出片前提：配音必须成套，缺一段直接拒绝生成。
- 时长控制：目标 780 秒，超上限才用 `atempo` 轻微提速（上限 1.15，不改音调）。

### 3.6 代码规范与中文乱码根治

乱码的根因不在源码而在解释方式：源码是 UTF-8，Windows 中文控制台默认按 GBK(936) 解释字节，于是"欢迎"变成"娆㈣繋"。分两层解决：

| 层 | 做法 |
|:---|:---|
| 程序 | 每个 `main` 开头调用 `console_utf8_init()`，内部执行 `SetConsoleOutputCP(CP_UTF8)` |
| 脚本 | `build.bat` 第二行 `chcp 65001`，文件本身 UTF-8 + CRLF |

配套核查工具：`run_check.py`（构建 + 运行 + 体检，14/14 通过）、`_bat_all.py`（真实 cmd 跑所有 build.bat）、`verify_console.py`（证明代码页 936 → 65001）。

### 3.7 仓库规范化与交付

- 每讲顶层 `README.md` 由 `gen_lecture_readme.py` 统一生成，视频时长用 ffprobe 实测写入，避免手写数字过期。
- `.gitignore` 明确边界：入库源码 / 课件 / 成片 / 字幕 / 配音 / 思考题 / 构建脚本；不入库 `video/_work/`、`*.log`、`vbx*.json`、`__pycache__/` 等中间产物。
- 清理 `video/_work`（14 讲合计约 1.7G）、根目录 24 个日志、各讲临时文件。
- 逐讲提交、逐讲推送，提交信息用 `feat:` / `chore:` 前缀区分。

### 3.8 综合篇

在 14 讲之外增补一期综合篇，作为导学片与总复习：24 页课件 ↔ 24 段配音（3279 字）↔ 140 条硬字幕，成片 11.1 分钟；课件正本与生成脚本归档到新建的 `15_综合篇/`，根目录同时放一份与视频同名的 pptx 配对。

---

## 四、关键经验

### 4.1 流程与协作

用户说"第 N 讲是 X"时，先对照知识图谱总纲确认，不要直接动目录结构。曾因按字面理解"第 2 讲是表达式"而批量重命名 13 个讲次目录并全局改编号，最终大幅返工——目录重命名成本极高，且容易被 PowerPoint 渲染残留实例、预览程序占用而卡住。此后原则改为：优先移动内容，不重命名目录，操作前先停掉所有后台批次。

同一台机器上可能有其他会话的残留进程在跑同一批产物，会互相覆盖甚至把服务拖死。为每个工具加锁（`tools/_pipelock.py`，按工具分锁，靠 pid 存活判断自愈）；克隆与渲染互斥，不同时跑——同时跑时克隆会慢 6~8 倍。

后台长任务会被沙箱结束，所以整条流水线必须能断点续跑：分段结果缓存复用，指纹判断跳过已完成段，中断后重启即可接着做。

### 4.2 视频与音频

ffmpeg 中间文件必须按段命名（`seg_NN.silent.mp4`）。曾因所有并行进程共用同一个临时文件名而互相覆盖，产出被截断的分段，拼出来无声可放。

画面只允许逐帧亚像素推近。最初的"隔帧推近"是抖动的元凶，改成逐帧浮点仿射变换后，帧间差分从 0.08/0.35 交替降到均匀 0.08。

运行 `build_video.py` 时不要用 `--only N` 做验证——它会把已成片重写成单段视频。已加保护：带 `--only` 时自动输出到 `_work/试片.mp4`，不碰正式成片。

凡是生成物落在 `_work/`（被 gitignore）的，成片与课件必须显式复制到正式目录，否则等于没入库。

### 4.3 Windows 构建脚本的三个坑

| 坑 | 现象 | 正确写法 |
|:---|:---|:---|
| `>` 被当重定向 | `echo -> xxx` 把提示文字写进 `.o` / `.dll`，产物变成十几字节，这就是 09 / 10 讲一直构建失败的真正原因 | 写成 `-^>` |
| chcp 后接大段中文注释 | cmd 重新定位文件导致行解析错乱 | 精简 bat 里的中文注释块 |
| 保留设备名 `nul` | Makefile 里 `>nul` 真的生成名为 nul 的文件，回收站删不掉，`git add` 报错 | 写成 `cmd /c "chcp 65001 >nul 2>&1"` |

另需注意：主循环 `scanf` 必须检查返回值，否则输入耗尽即死循环，自动化演示必卡住。

### 4.4 Git 相关

文件一旦被 git 跟踪，再写进 `.gitignore` 也没用，必须 `git rm -r --cached`。`video/_work` 就是这样混进了 1.7G。批量 `git rm` 时若某个路径不存在会整条 fatal 中断，需逐路径循环执行。

---

## 五、成果清单

| 项目 | 数量 / 说明 |
|:---|:---|
| 讲次 | 14 讲 + 15_综合篇，全部完成 |
| 源代码 | 14 讲自包含源码，`gcc -Wall` 全部编译运行通过 |
| 课件 | 每讲 18 页统一结构 + 综合篇 24 页 |
| 视频 | 每讲 8.9~10.1 分钟（14 讲实测） + 综合篇 11.1 分钟，均带硬字幕、鼠标指示与 `.srt` |
| 讲解稿 | 每讲 18 段、2870~2930 字 |
| 思考题 | 每讲 4 道（含解答） |
| 知识图谱 | K01~K14 共 85 个知识节点，跨讲依赖完整 |
| 工具链 | `tools/` 下 20 余个脚本，覆盖排版、出片、配音、字幕、体检、归档 |

主要提交（近 5 次）：

```
f1b8263 feat: 综合篇课件归档 —— 24 页 PPTX + 讲解稿入库
4f910e0 feat: 综合篇视频 —— 从表达式到框架，14 讲要点一次讲透
c319e29 chore: 清理日志与中间产物，统一 14 讲目录规范
91de69a chore: 同步最新构建产物（14 讲全部重新编译）
fd5fba6 chore: 删除 13 讲 src 下误写入的垃圾文件
```

---

## 六、一句话总结

这套项目的关键不在"生成内容"，而在"建立可重复的内容生产线"：先用知识图谱锁定讲什么，再用固定目录结构锁定每讲产出什么，然后用自研工具链与质量门禁锁定怎么产出，最后用指纹、缓存、锁把这条线做成可断点续跑、可逐讲交付的流水线。
